---
phase: quick
plan: "260405-0d4"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - src/auth/outlook-oauth.ts
  - src/auth/oauth.ts
  - src/cli.ts
  - src/email/composer.ts
  - src/email/parser.ts
  - src/providers/gmail-provider.ts
  - src/providers/outlook-provider.ts
  - src/utils/config.ts
  - src/utils/errors.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npx tsc` exits with code 0 (no TypeScript errors)"
  artifacts:
    - path: "tsconfig.json"
      contains: '"types": [\"bun\"]'
  key_links:
    - from: "src/cli.ts"
      to: "src/providers/gmail-provider.ts, src/providers/outlook-provider.ts"
      via: "type import"
---

<objective>
Fix all TypeScript compilation errors so `npx tsc` passes cleanly.
</objective>

<execution_context>
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Errors from `npx tsc`:

### Root cause: tsconfig missing bun types
- Cannot find name 'Bun' in config.ts, outlook-oauth.ts, cli.ts
- Cannot find module 'bun:test' in composer.test.ts, parser.test.ts, gmail-provider.test.ts
- Cannot find module 'bun:fs' in outlook-oauth.ts

### Fix 1: outlook-oauth.ts line 1
`CachePlugin` is not exported from `@azure/msal-node` — should be `ICachePlugin`

### Fix 2: cli.ts line 38
`GmailProvider` and `OutlookProvider` used but not imported. Need to add imports.

### Fix 3: cli.ts line 50
`accounts[0]` is `string | undefined` due to `noUncheckedIndexedAccess`. Use non-null assertion: `accounts[0]!`

### Fix 4: email-provider.ts list() signature mismatch
Base class `EmailProvider.list()` returns `Promise<Email[]>`. GmailProvider and OutlookProvider return `{ emails: Email[], nextPageToken?: string }`. The return types must match the base class.

### Fix 5: gmail-provider.ts line 88
Email objects missing `to` field (required by Email interface). Also `snippet` is `string | null | undefined` but `from` expects `string | undefined`.

### Fix 6: @types/mailparser missing
parser.ts imports mailparser but @types/mailparser not installed.

### Fix 7: errors.ts line 16
`...` spread on `unknown` type not allowed. Fix: use explicit conditional.

### Fix 8: composer.ts line 42
`mailComposer.compile().build()` returns `Promise<Buffer>` but code treats as sync Buffer. Need `await`.

### Fix 9: oauth.ts line 76
`userData` typed as `unknown` — cast to proper shape.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tsconfig.json and install missing @types</name>
  <files>tsconfig.json, package.json</files>
  <action>
    1. Add `"types": ["bun"]` to tsconfig.json compilerOptions (enables Bun global types)
    2. Run `bun add -d @types/mailparser` to install missing type declarations
    3. Verify tsconfig.json now has:
       ```json
       "compilerOptions": {
         "types": ["bun"],
         ...
       }
       ```
  </action>
  <verify>npx tsc 2>&1 | head -5 | grep -E "bun|mailparser" || echo "bun/mailparser errors resolved"</verify>
  <done>tsconfig.json has `"types": ["bun"]` and @types/mailparser is in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Fix outlook-oauth.ts, oauth.ts, errors.ts</name>
  <files>src/auth/outlook-oauth.ts, src/auth/oauth.ts, src/utils/errors.ts</files>
  <action>
    **outlook-oauth.ts:**
    - Line 1: Change `CachePlugin` to `ICachePlugin` (it's an interface, not a class export)
    - Lines 47, 50: Add types to `cacheContext` parameter — after Bun types are available, the MSAL types should work. If still any, type as `ICacheContext` from msal.

    **oauth.ts line 76:**
    - Cast userData.json() result: `const userData = (await userInfo.json()) as { email?: string };`

    **errors.ts line 16:**
    - Change spread from `...(this.details && { details: this.details })` to explicit conditional:
    ```typescript
    const detailsObj = this.details ? { details: this.details } : {};
    ```
    And use `...detailsObj` instead of the spread expression.
  </action>
  <verify>npx tsc 2>&1 | grep -E "oauth|errors" | head -10</verify>
  <done>oauth.ts and errors.ts TypeScript errors resolved</done>
</task>

<task type="auto">
  <name>Task 3: Fix cli.ts imports and account assignment</name>
  <files>src/cli.ts</files>
  <action>
    **cli.ts imports (near top after existing imports):**
    Add:
    ```typescript
    import { GmailProvider } from "./providers/gmail-provider.js";
    import { OutlookProvider } from "./providers/outlook-provider.js";
    ```

    **cli.ts line 50:**
    - Change `account = accounts[0];` to `account = accounts[0]!;` (non-null assertion since we already checked length >= 1)
  </action>
  <verify>npx tsc 2>&1 | grep "cli.ts" | head -10</verify>
  <done>cli.ts TypeScript errors resolved</done>
</task>

<task type="auto">
  <name>Task 4: Fix provider list() signature mismatches</name>
  <files>src/providers/email-provider.ts, src/providers/gmail-provider.ts, src/providers/outlook-provider.ts</files>
  <action>
    **Root issue:** Base class `EmailProvider.list()` returns `Promise<Email[]>` but GmailProvider and OutlookProvider return `{ emails: Email[], nextPageToken?: string }`.

    **Fix approach:** Change base class to match actual provider behavior since pagination token is a real feature needed by scripts.

    In `email-provider.ts`:
    ```typescript
    abstract list(folder?: string, limit?: number): Promise<{ emails: Email[]; nextPageToken?: string }>;
    ```

    In `gmail-provider.ts`:
    - Line 38: Return type is already correct `{ emails: Email[], nextPageToken?: string }`
    - Line 88: Add missing `to` field to email objects: `to: []` (empty array since Gmail list doesn't include recipients)
    - Line 88: Fix `snippet` nullability: use `detail.data.snippet || ""` instead of `detail.data.snippet`

    In `outlook-provider.ts`:
    - Line 70: Same return type issue — verify return matches base class signature
    - Similar `to` field issue if applicable

    **IMPORTANT:** The `Email` interface requires `to: string[]` (array, not optional). Ensure all returned email objects have `to: []` for list operations where recipient data is not available.
  </action>
  <verify>npx tsc 2>&1 | grep -E "gmail-provider|outlook-provider|email-provider" | head -15</verify>
  <done>Provider type mismatches resolved, npx tsc shows no provider-related errors</done>
</task>

<task type="auto">
  <name>Task 5: Fix remaining type errors (parser, composer)</name>
  <files>src/email/parser.ts, src/email/composer.ts</files>
  <action>
    **parser.ts:**
    - Lines 38, 47: After @types/mailparser is installed, the implicit `any` on `att`, `idx`, `a` should resolve. If still any errors, add explicit types:
      - `(att: Attachment, idx: number)`
      - `(a: Address)` where `Address` comes from mailparser types

    **composer.ts line 42:**
    - The `build()` method in nodemailer is async. Change:
      ```typescript
      // Before:
      const messageBuffer = mailComposer.compile().build() as Buffer;
      // After:
      const messageBuffer = await mailComposer.compile().build();
      ```
    - Also update `base64UrlEncode` call below to `await` since build() is now async.
    - Actually, `buildRawMessage` is a sync function — we need to make `buildRawMessage` async or handle differently. Since `build()` returns a Promise, we must make `buildRawMessage` async and await the build call.
  </action>
  <verify>npx tsc 2>&1 | grep -E "parser|composer" | head -10</verify>
  <done>All email module TypeScript errors resolved</done>
</task>

<task type="auto">
  <name>Task 6: Final verification</name>
  <files>tsconfig.json</files>
  <action>
    Run `npx tsc` and confirm zero errors (exit code 0).
  </action>
  <verify>npx tsc 2>&1; echo "Exit code: $?"</verify>
  <done>`npx tsc` exits with code 0 — all TypeScript errors fixed</done>
</task>

</tasks>

<verification>
`npx tsc` produces no errors (exit code 0).
</verification>

<success_criteria>
All 26+ TypeScript errors from `npx tsc` are resolved. The command exits with code 0.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0d4-fix-all-typescript-error-npx-tsc/260405-0d4-SUMMARY.md`
</output>
