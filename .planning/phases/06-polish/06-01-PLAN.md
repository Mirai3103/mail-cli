---
phase: 06-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/config.ts
  - src/utils/index.ts
autonomous: true
requirements:
  - ORG-05

must_haves:
  truths:
    - "CLI reads OAuth client credentials from ~/.emailcli/config.json"
    - "Config file auto-creates with empty values if not present"
    - "Environment variables GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET, OUTLOOK_CLIENT_ID, OUTLOOK_CLIENT_SECRET override config file"
  artifacts:
    - path: "src/utils/config.ts"
      provides: "Config loading with env override and auto-create"
      min_lines: 30
    - path: "~/.emailcli/config.json"
      provides: "Default config file at user home directory"
      auto_created: true
  key_links:
    - from: "src/cli.ts"
      to: "src/utils/config.ts"
      via: "import { loadConfig } from './utils/config.js'"
      pattern: "loadConfig"
---

<objective>
Create config file system at ~/.emailcli/config.json with auto-creation and environment variable override support.
</objective>

<execution_context>
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/utils/errors.ts
@src/cli.ts

## Config Schema (per D-07)
```typescript
interface Config {
  gmail: {
    clientId: string;
    clientSecret: string;
  };
  outlook: {
    clientId: string;
    clientSecret: string;
  };
}
```

## Key Decisions from 06-CONTEXT.md
- **D-04:** Config stored at `~/.emailcli/config.json`
- **D-05:** Config file created with empty values if not exists (auto-create on first run)
- **D-06:** Env vars override config file values: GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET, OUTLOOK_CLIENT_ID, OUTLOOK_CLIENT_SECRET
- **D-07:** Config schema: `{ "gmail": { "clientId": "", "clientSecret": "" }, "outlook": { "clientId": "", "clientSecret": "" } }`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.ts with loadConfig function</name>
  <read_first>
    - src/utils/errors.ts
    - src/cli.ts
  </read_first>
  <action>
    Create `src/utils/config.ts` with:

1. `Config` interface matching D-07 schema:
   ```typescript
   interface Config {
     gmail: { clientId: string; clientSecret: string };
     outlook: { clientId: string; clientSecret: string };
   }
   ```

2. `getConfigPath()` function returning `~/.emailcli/config.json` using `os.homedir()` and path.join

3. `ensureConfigDir()` function that creates `~/.emailcli/` directory if it doesn't exist using Bun's `Bun.file` and directory creation

4. `loadConfig()` function that:
   - Gets config path via `getConfigPath()`
   - Checks if config file exists using `Bun.file(path).exists()`
   - If NOT exists: creates it with default empty schema using `Bun.write()` with content `{"gmail":{"clientId":"","clientSecret":""},"outlook":{"clientId":"","clientSecret":""}}}`
   - Reads existing config using `Bun.file(path).text()` and `JSON.parse()`
   - Applies env var overrides (D-06):
     - `process.env.GMAIL_CLIENT_ID` overrides `config.gmail.clientId` if set
     - `process.env.GMAIL_CLIENT_SECRET` overrides `config.gmail.clientSecret` if set
     - `process.env.OUTLOOK_CLIENT_ID` overrides `config.outlook.clientId` if set
     - `process.env.OUTLOOK_CLIENT_SECRET` overrides `config.outlook.clientSecret` if set
   - Returns the resolved Config object

5. Export `loadConfig` and `Config` type
  </action>
  <acceptance_criteria>
    - grep exports "loadConfig" in src/utils/config.ts
    - grep defines "interface Config" in src/utils/config.ts
    - grep uses "GMAIL_CLIENT_ID" for gmail.clientId override
    - grep uses "OUTLOOK_CLIENT_ID" for outlook.clientId override
    - grep uses "Bun.file" for file existence check
    - grep uses "Bun.write" for file creation
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck src/utils/config.ts</automated>
  </verify>
  <done>loadConfig() function created with auto-create and env override, exports Config type and loadConfig</done>
</task>

<task type="auto">
  <name>Task 2: Export loadConfig from utils/index.ts</name>
  <read_first>
    - src/utils/index.ts
  </read_first>
  <action>
    Add to `src/utils/index.ts`:
    ```typescript
    export { loadConfig } from "./config.js";
    ```
  </action>
  <acceptance_criteria>
    - grep exports "loadConfig" in src/utils/index.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck src/utils/index.ts</automated>
  </verify>
  <done>loadConfig exported from utils/index.ts</done>
</task>

</tasks>

<verification>
- Config file created at ~/.emailcli/config.json with empty values on first load
- Env vars GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET override config values
- No existing command functionality broken (all tests still pass)
</verification>

<success_criteria>
- `loadConfig()` returns config object with gmail/outlook clientId/clientSecret
- Config file auto-created if missing with empty schema
- Env vars take precedence over config file values
- Config type exported for use by other modules
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-01-SUMMARY.md`
</output>
