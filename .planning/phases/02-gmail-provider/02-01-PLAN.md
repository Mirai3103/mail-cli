---
phase: 02-gmail-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/email-provider.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - ORG-04
must_haves:
  truths:
    - "User can list emails with essential fields (id, from, subject, date, flags)"
    - "User can get mailbox status (unread count, total messages)"
    - "User can list folders with type distinction (system vs user)"
  artifacts:
    - path: "src/providers/email-provider.ts"
      provides: "Folder interface with type field"
      contains: "type?: \"system\" | \"user\""
    - path: "src/providers/email-provider.ts"
      provides: "EmailProvider abstract class with status() method"
      contains: "abstract status(): Promise<{ unread: number; total: number }>"
  key_links:
    - from: "src/providers/gmail-provider.ts"
      to: "src/providers/email-provider.ts"
      via: "extends EmailProvider"
      pattern: "extends EmailProvider"
---

<objective>
Update the EmailProvider abstract interface to support Phase 2 Gmail provider implementation. Add the `status()` method and extend the `Folder` interface with `type` field.
</objective>

<execution_context>
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/workflows/execute-plan.md
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/providers/email-provider.ts
</context>

<introduction>
Phase 2 implements Gmail API integration. This plan updates the abstract interface to add:
1. `status()` method - returns unread/total counts (per D-09, D-10)
2. `type` field on Folder - distinguishes system vs user labels (per D-07)

These changes enable GmailProvider to implement NAV-01, NAV-02, NAV-03, and ORG-04.
</introduction>

<tasks>

<task type="auto">
  <name>Task 1: Add type field to Folder interface</name>
  <files>src/providers/email-provider.ts</files>
  <read_first>src/providers/email-provider.ts</read_first>
  <action>
    Update the Folder interface in src/providers/email-provider.ts to add the `type` field:

    ```typescript
    export interface Folder {
      id: string;
      name: string;
      type?: "system" | "user";
    }
    ```

    The `type` field is optional to maintain backward compatibility with any existing code that creates Folder objects without it. GmailProvider will always populate this field when implementing listFolders().
  </action>
  <verify>
    <automated>grep -c "type?: \"system\" | \"user\";" src/providers/email-provider.ts</automated>
  </verify>
  <done>Folder interface has type field with correct type union</done>
  <acceptance_criteria>
    - Folder interface in src/providers/email-provider.ts contains: `type?: "system" | "user";`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add status() method to EmailProvider abstract class</name>
  <files>src/providers/email-provider.ts</files>
  <read_first>src/providers/email-provider.ts</read_first>
  <action>
    Add the abstract `status()` method to the EmailProvider class in src/providers/email-provider.ts.

    Add this method signature after the existing abstract methods (before the listFolders line):

    ```typescript
    // Mailbox status
    abstract status(): Promise<{ unread: number; total: number }>;
    ```

    This method returns the unread count and total message count for the inbox. Per D-09 and D-10, this reflects INBOX specifically.
  </action>
  <verify>
    <automated>grep -c "abstract status():" src/providers/email-provider.ts</automated>
  </verify>
  <done>EmailProvider has abstract status() method</done>
  <acceptance_criteria>
    - EmailProvider abstract class contains: `abstract status(): Promise<{ unread: number; total: number }>;`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- grep verifies Folder interface has type field
- grep verifies EmailProvider has status() method
- Both changes in same file, committed together
</verification>

<success_criteria>
- Folder interface has `type?: "system" | "user"` field
- EmailProvider has `abstract status(): Promise<{ unread: number; total: number }>` method
- No TypeScript errors from interface change
</success_criteria>

<output>
After completion, create `.planning/phases/02-gmail-provider/{phase}-01-SUMMARY.md`
</output>
