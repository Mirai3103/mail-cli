---
phase: 09-logging-quality
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
files_modified:
  - biome.json
  - src/commands/*.ts
  - src/services/*.ts
  - src/infrastructure/*.ts
  - src/providers/*.ts
  - src/http/*.ts
  - src/email/*.ts
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "`bun run lint` passes with zero warnings"
    - "`bun run format` produces consistent formatting"
    - "biome.json is properly configured for this project"
  artifacts:
    - path: "biome.json"
      provides: "Lint and format configuration"
    - path: "src/commands/"
      provides: "Linted and formatted command files"
    - path: "src/services/"
      provides: "Linted and formatted service files"
  key_links:
    - from: "package.json"
      to: "biome.json"
      via: "scripts reference biome commands"
    - from: "biome.json"
      to: "src/**/*.ts"
      via: "lint and format applied to all source files"
---

<objective>
Set up Biome configuration and fix all lint warnings across the codebase. Ensure `bun run lint` passes with zero warnings and `bun run format` produces consistent formatting.
</objective>

<execution_context>
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/workflows/execute-plan.md
@/home/laffy/Desktop/mail-cli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/laffy/Desktop/mail-cli/biome.json
@/home/laffy/Desktop/mail-cli/package.json
@/home/laffy/Desktop/mail-cli/src/commands/list.ts
@/home/laffy/Desktop/mail-cli/src/commands/read.ts
</context>

<interfaces>
Current biome.json configuration:
```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.10/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": { "ignoreUnknown": false },
  "formatter": { "enabled": true, "indentStyle": "tab" },
  "linter": { "enabled": true, "rules": { "recommended": true } },
  "javascript": { "formatter": { "quoteStyle": "double" } },
  "assist": { "enabled": true, "actions": { "source": { "organizeImports": "on" } } }
}
```

package.json scripts:
```json
{
  "lint": "bunx --bun @biomejs/biome lint --write",
  "format": "bunx --bun @biomejs/biome format --write"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run format and lint to identify issues</name>
  <files>biome.json</files>
  <action>
First, run the current lint and format to see what issues exist:

```bash
cd /home/laffy/Desktop/mail-cli
bun run format
bun run lint
```

Review the output to identify:
1. Any formatting issues (indent style conflicts, quote style)
2. Lint warnings or errors (unused imports, missing exports, etc.)

Then update biome.json if needed based on findings. The current config uses tabs for indentation and double quotes, which should match the existing codebase. If there are conflicts, adjust accordingly.

Common issues to watch for:
- Quote style mismatches (single vs double)
- Missing explicit return types on exported functions
- Unused imports
- Missing destructuring patterns
  </action>
  <verify>
    <automated>cd /home/laffy/Desktop/mail-cli && bun run lint 2>&1 | head -50</automated>
  </verify>
  <done>All lint warnings identified and fixed. bun run lint shows zero warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Replace magic numbers in providers with constants</name>
  <files>src/providers/gmail-provider.ts, src/providers/outlook-provider.ts, src/infrastructure/gmail-provider.ts, src/infrastructure/outlook-provider.ts</files>
  <action>
Replace magic numbers in provider files with named constants from `src/utils/constants.ts`:

**In each provider file, add to imports:**
```typescript
import { DEFAULT_LIMIT, MAX_LIMIT, clampLimit } from "../utils/constants.js";
```

**Replace patterns like:**
- `limit: number = 20` -> `limit: number = DEFAULT_LIMIT`
- `Math.min(Math.max(1, limit), 100)` -> `clampLimit(limit)`

Files to update:
1. src/providers/gmail-provider.ts - lines with `limit = 20` and `Math.min(Math.max(1, limit), 100)`
2. src/providers/outlook-provider.ts - same patterns
3. src/infrastructure/gmail-provider.ts - same patterns
4. src/infrastructure/outlook-provider.ts - same patterns

Check each file for these patterns:
```typescript
// Replace:
limit: number = 20
// With:
limit: number = DEFAULT_LIMIT

// Replace:
Math.min(Math.max(1, limit), 100)
// With:
clampLimit(limit)
```
  </action>
  <verify>
    <automated>cd /home/laffy/Desktop/mail-cli && bun run lint --files src/providers/*.ts src/infrastructure/*.ts 2>&1 | head -20</automated>
  </verify>
  <done>All magic numbers in provider files replaced with named constants.</done>
</task>

<task type="auto">
  <name>Task 3: Replace magic numbers in services and commands</name>
  <files>src/services/mailbox-service.ts, src/services/email-service.ts, src/services/compose-service.ts, src/commands/list.ts, src/commands/search.ts</files>
  <action>
Replace magic numbers in service and command files with named constants:

**In each file, add to imports:**
```typescript
import { DEFAULT_LIMIT, MAX_LIMIT } from "../utils/constants.js";
```

**Replace:**
- `limit: number = 20` -> `limit: number = DEFAULT_LIMIT`
- In services where limit is validated with `Math.min(Math.max(1, limit), 100)`, use clampLimit

Files to update:
1. src/services/mailbox-service.ts - list() default parameter
2. src/services/email-service.ts - search() default parameter
3. src/commands/list.ts - option default "20" and parseInt
4. src/commands/search.ts - option default "20" and parseInt
5. Any other files with limit defaults

Note: Commands use string option values ("20"), so the command option string stays as-is but should be parsed as DEFAULT_LIMIT. Consider refactoring commands to import and use DEFAULT_LIMIT constant for validation.
  </action>
  <verify>
    <automated>cd /home/laffy/Desktop/mail-cli && bun run lint --files src/services/*.ts src/commands/list.ts src/commands/search.ts 2>&1 | head -20</automated>
  </verify>
  <done>Magic numbers in services and commands replaced with named constants.</done>
</task>

<task type="auto">
  <name>Task 4: Final lint and format verification</name>
  <files>biome.json</files>
  <action>
Run full lint and format check across all source files:

```bash
cd /home/laffy/Desktop/mail-cli
bun run format
bun run lint
```

Fix any remaining issues:
- Address any unused imports (remove or use)
- Address any missing return types on exported functions
- Address any type annotation issues

Verify no warnings or errors remain.
  </action>
  <verify>
    <automated>cd /home/laffy/Desktop/mail-cli && bun run lint 2>&1</automated>
  </verify>
  <done>bun run lint passes with zero warnings. bun run format produces consistent formatting.</done>
</task>

</tasks>

<verification>
Run `bun run lint` and `bun run format` to verify all quality requirements met.
</verification>

<success_criteria>
- biome.json properly configured
- `bun run format` produces consistent formatting (tabs, double quotes)
- `bun run lint` passes with zero warnings across all source files
- All magic numbers replaced with named constants
- No unused imports or missing return types on exported functions
</success_criteria>

<output>
After completion, create `.planning/phases/09-logging-quality/09-03-SUMMARY.md`
</output>
