---
phase: 10-cicd-docs
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - CICD-01
  - CICD-02
  - CICD-03
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "GitHub Actions workflow triggers on push to main/feat/* and pull requests to main"
    - "CI pipeline runs lint and tests on every push"
    - "CI pipeline uploads coverage report as artifact"
    - "CONTRIBUTING.md explains how to run tests and lint locally"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      min_lines: 45
    - path: "CONTRIBUTING.md"
      provides: "Contribution guidelines with local dev instructions"
      min_lines: 50
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "bun run lint"
      via: "run step"
      pattern: "bun run lint"
    - from: ".github/workflows/ci.yml"
      to: "bun test"
      via: "run step"
      pattern: "bun test --coverage"
---

<objective>
Set up automated CI/CD pipeline with GitHub Actions and create CONTRIBUTING.md guidelines.
</objective>

<context>
@package.json (scripts for lint/test/format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with:
- Trigger on: push to main/feat/* branches, pull requests to main
- Job: test-and-lint on ubuntu-latest
- Steps:
  1. actions/checkout@v4
  2. oven-sh/setup-bun@v2 for Bun runtime
  3. bun install
  4. bun run lint (runs biome check)
  5. bun test --coverage --reporter=verbose
  6. actions/upload-artifact@v4 to upload coverage/ directory

Use specific artifact name "coverage-report" and path "coverage/".
</action>
  <verify>File exists at .github/workflows/ci.yml with all 6 steps, trigger events, and artifact upload</verify>
  <done>CI workflow file created and validates against GitHub Actions schema</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create CONTRIBUTING.md with sections:
- Setup: clone, bun install, verify setup
- Code Style: bun run format (auto-format), bun run lint (check)
- Testing: bun test (run tests), bun test --coverage (coverage report), minimum 80% for business logic
- Making Changes: feature branch workflow, ensure lint/tests pass, submit PR
- Development: explain the Clean Architecture layers (commands/, services/, infrastructure/, types/)
- Commands reference: short list of available CLI commands

Minimum 50 lines of substantive content.
</action>
  <verify>File exists at CONTRIBUTING.md with all sections, minimum 50 lines</verify>
  <done>CONTRIBUTING.md created with complete contribution guidelines</done>
</task>

</tasks>

<verification>
bun run lint passes locally before workflow is tested
</verification>

<success_criteria>
1. CI workflow file created at .github/workflows/ci.yml
2. Workflow triggers on correct branches
3. Lint and test steps present
4. Coverage artifact upload configured
5. CONTRIBUTING.md exists with testing/linting instructions
</success_criteria>

<output>
After completion, create `.planning/phases/10-cicd-docs/10-01-SUMMARY.md`
</output>