---
phase: 10-cicd-docs
plan: '02'
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ARCHITECTURE.md
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
user_setup: []

must_haves:
  truths:
    - "Architecture diagram shows Clean Architecture layers and data flow"
    - "README.md reflects the new folder structure and Clean Architecture"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Architecture diagram with Clean Architecture layers and data flow"
      min_lines: 60
    - path: "README.md"
      provides: "Project documentation updated with new architecture"
      min_lines: 120
  key_links:
    - from: "ARCHITECTURE.md"
      to: "src/commands/"
      via: "diagram reference"
      pattern: "commands/"
    - from: "README.md"
      to: "src/services/"
      via: "documentation reference"
      pattern: "services/"
---

<objective>
Create architecture diagram and update README.md to reflect the new folder structure.
</objective>

<context>
@src/commands/index.ts (command exports)
@src/services/index.ts (service exports)
@src/infrastructure/index.ts (infrastructure exports)
@src/types/ (type definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARCHITECTURE.md with diagram</name>
  <files>ARCHITECTURE.md</files>
  <action>
Create ARCHITECTURE.md with:
1. Overview section explaining Clean Architecture approach
2. ASCII diagram showing 4 layers:
   - Presentation Layer (CLI): src/commands/ (list, read, send, search, etc.)
   - Application Layer: src/services/ (MailboxService, EmailService, ComposeService, AccountService)
   - Domain Layer: src/types/ (port interfaces: EmailProviderPort, TokenStoragePort, ConfigPort)
   - Infrastructure Layer: src/infrastructure/ (GmailProvider, OutlookProvider, TokenStorageImpl, ConfigImpl)
3. Data flow description: CLI command -> service -> provider -> API
4. Port interfaces section showing how layers connect
5. Key design decisions: dependency injection, swappable providers, no commander in services

Diagram format:
```
┌─────────────────────────────────────────────────────────────┐
│                 CLI Presentation Layer                       │
│    list.ts  read.ts  send.ts  search.ts  ... (commands)    │
└────────────────────────────┬────────────────────────────────┘
                             │ depends on
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                Application Services Layer                    │
│  MailboxService  EmailService  ComposeService  AccountSvc  │
└────────────────────────────┬────────────────────────────────┘
                             │ depends on
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    Domain Types Layer                       │
│    EmailProviderPort  TokenStoragePort  ConfigPort  ...    │
└────────────────────────────┬────────────────────────────────┘
                             │ implements
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  Infrastructure Layer                         │
│    GmailProvider  OutlookProvider  TokenStorageImpl  ...    │
└─────────────────────────────────────────────────────────────┘
```

Minimum 60 lines with substantive content.
</action>
  <verify>File exists at ARCHITECTURE.md, contains ASCII diagram with 4 layers, minimum 60 lines</verify>
  <done>Architecture diagram created with Clean Architecture layers and data flow</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with folder structure</name>
  <files>README.md</files>
  <action>
Update README.md to reflect the new Clean Architecture folder structure:
1. Add "Architecture" section explaining the 4 layers (presentation, application, domain, infrastructure)
2. Add "Project Structure" section showing the folder layout:
   - src/commands/ - CLI command handlers
   - src/services/ - Business logic (use cases)
   - src/types/ - Port interfaces and domain types
   - src/infrastructure/ - Provider implementations (Gmail, Outlook)
3. Keep existing content: installation, setup, commands reference
4. Add "Development" section referencing CONTRIBUTING.md for local dev
5. Keep existing examples for list, read, send, search, etc.

Preserve all existing functionality documentation. Add the new architecture content after the Quick Start section.
</action>
  <verify>File exists at README.md, updated with architecture section and project structure, minimum 120 lines</verify>
  <done>README.md updated with Clean Architecture folder structure and project layout</done>
</task>

</tasks>

<verification>
README.md contains "Architecture" section with folder structure
ARCHITECTURE.md contains valid ASCII diagram
</verification>

<success_criteria>
1. ARCHITECTURE.md created with ASCII diagram showing 4 layers
2. README.md updated with architecture and project structure sections
3. Both documents reference the correct folder paths
</success_criteria>

<output>
After completion, create `.planning/phases/10-cicd-docs/10-02-SUMMARY.md`
</output>